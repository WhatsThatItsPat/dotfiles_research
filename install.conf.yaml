- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    ~/.gitconfig:
    ~/.config/bat:
      create: true
      path: config/bat

#- create:
#    - ~/Developer
#    - ~/Work

- shell:
  - command: ./setup-homebrew.sh
    stdout: true
    stderr: true
  - command: ./setup-zsh.zsh
    stdout: true
    stderr: true
  - command: ./setup-node.zsh
    stdout: true
    stderr: true
