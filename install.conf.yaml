- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
    ~/.ssh/config: ssh/config

- create:
    - ~/Developer
    - ~/Work

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
