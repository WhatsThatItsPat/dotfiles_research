- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.gitconfig: gitconfig
#    ~/.ssh/config: ssh/config

#- create:
#    - ~/Developer
#    - ~/Work

- shell:
  - command: ./setup-homebrew.sh
    stdout: true
    stderr: true
